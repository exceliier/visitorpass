{
  "name": "visitorpass-fullstack",
  "version": "1.0.0",
  "description": "Fullstack Visitor Pass Management System (Backend + Frontend)",
  "scripts": {
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix vp_frontend",
    "dev": "npx concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix vp_frontend\"",
    "start:fullstack": "npx concurrently --kill-others-on-fail \"npm start --prefix backend\" \"npm run dev --prefix vp_frontend\"",
    "build:frontend": "npm run build --prefix vp_frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
